---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: awx-projects-claim
  namespace: awx 
spec:
  accessModes:
  - ReadWriteMany
  storageClassName: awx
  resources:
    requests:
      storage: 1Ti