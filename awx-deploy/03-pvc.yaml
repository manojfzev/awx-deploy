---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: awx-projects-claim
  namespace: awx 
spec:
  accessModes:
  - ReadWriteMany
  storageClassName: awx-postgres-volume
  resources:
    requests:
      storage: 20Gi
